# Perceptron

## p.py

```bash
$ python p.py --help
usage: p.py [-h] [--alpha ALPHA] [--epoch EPOCH]

optional arguments:
  -h, --help     show this help message and exit
  --alpha ALPHA  learning rate
  --epoch EPOCH  epoch
```
